package fr.isep.studycord;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration tests that verify the Spring application context starts successfully.
 */
@SpringBootTest
class StudycordApplicationTests {

	/**
	 * Ensures the application context loads without errors.
	 */
	@Test
	void contextLoads() {
	}

}
